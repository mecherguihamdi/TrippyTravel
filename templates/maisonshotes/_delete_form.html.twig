<form method="post" action="{{ path('maisonshotes_delete', {'id': maisonshote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maisonshote.id) }}">
    <button class="btn">Delete</button>
</form>
