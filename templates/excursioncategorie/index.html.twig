{% extends 'baseBO.html.twig' %}

{% block title %}Excursioncategorie index{% endblock %}

{% block body %}
    <div class="main-panel">
        <div class="content-wrapper">
            <div class="row">
                <div class="col-md-12 grid-margin stretch-card">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title">Excursion catégories</h4>
                            <p class="card-description">
                                <a class="btn btn-primary" href="{{ path('excursioncategorie_new') }}">Ajouter</a>
                            </p>
                            <div class="table-responsive pt-3">
                                <table class="table table-bordered">
                                    <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Libelle</th>
                                        <th>actions</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for excursioncategorie in excursioncategories %}
                                        <tr>
                                            <td>{{ excursioncategorie.id }}</td>
                                            <td>{{ excursioncategorie.libelle }}</td>
                                            <td>
                                                <a class="btn btn-info"  href="{{ path('excursioncategorie_show', {'id': excursioncategorie.id}) }}">Afficher</a>
                                                <a class="btn btn-dark btn-icon-text" href="{{ path('excursioncategorie_edit', {'id': excursioncategorie.id}) }}">Modifier</a>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="3">no records found</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
