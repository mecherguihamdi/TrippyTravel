{% extends 'base.html.twig' %}

{% block title %}Excursioncategorie index{% endblock %}

{% block body %}
    <h1>Excursioncategorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for excursioncategorie in excursioncategories %}
            <tr>
                <td>{{ excursioncategorie.id }}</td>
                <td>{{ excursioncategorie.libelle }}</td>
                <td>
                    <a href="{{ path('excursioncategorie_show', {'id': excursioncategorie.id}) }}">show</a>
                    <a href="{{ path('excursioncategorie_edit', {'id': excursioncategorie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('excursioncategorie_new') }}">Create new</a>
{% endblock %}
