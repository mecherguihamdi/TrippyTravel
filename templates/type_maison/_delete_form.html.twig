<form method="post" action="{{ path('type_maison_delete', {'id': type_maison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_maison.id) }}">
    <button class="btn">Delete</button>
</form>
