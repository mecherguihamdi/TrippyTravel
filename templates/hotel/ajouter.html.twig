{% extends 'baseBO.html.twig' %}


{% block body %}

                            <div
                                  <div class="row">
                                  <h3 class="mt-5 mb-5">create form</h3>
                                  <div class="col-md-6 offset-md-3">
                                    
                                {{ form_start(form)}}
                                {{form(form)}}                 
                                {{ form_end(form)}}
                                  </div>
                            </div>
                    </div>

{% endblock %}