{% extends 'baseBO.html.twig' %}

{% block title %}Excursionreservation{% endblock %}

{% block body %}
    <div class="main-panel">
        <div class="content-wrapper">
            <div class="row">
                <div class="col-md-12 grid-margin stretch-card">
                    <div class="card">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <h4 class="card-title">Excursion réservation</h4>
                                </div>
                                <div class="col-md-6 text-right">
                                    <div class="btn-group">
                                        <a href="{{ path('excursionreservation_index') }}"
                                           class="btn btn-outline-light btn-icon-text"><i
                                                    class="mdi mdi-arrow-left btn-icon-prepend"></i></a>
                                        {{ include('excursionreservation/_delete_form.html.twig') }}
                                    </div>
                                </div>
                            </div>
                            <div class="table-responsive pt-3">
                                <table class="table">
                                    <tbody>
                                    <tbody>
                                    <tr>
                                        <th>Id</th>
                                        <td>{{ excursionreservation.id }}</td>
                                    </tr>
                                    <tr>
                                        <th>Prix</th>
                                        <td>{{ excursionreservation.prix }}</td>
                                    </tr>
                                    <tr>
                                        <th>Status</th>
                                        <td>{{ excursionreservation.status }}</td>
                                    </tr>
                                    <tr>
                                        <th>CreatedAt</th>
                                        <td>{{ excursionreservation.createdAt ? excursionreservation.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    </tr>
                                    </tbody>
                                </table>


                            </div>

                            <a class="btn btn-outline-primary btn-icon-text"
                               href="{{ path('excursionreservation_edit', {'id': excursionreservation.id}) }}"><i
                                        class="mdi mdi-grease-pencil"></i></a>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
