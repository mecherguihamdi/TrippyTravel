{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_widget(form) }}
<div class="form-group">
    <a  id="add_documentation" data-prototype-add="div#appbundle_excursion_excursionimages" class="btn btn-outline-danger btn-icon-text btn-collection-add">
        <i class="mdi mdi-library-plus"></i>
        Ajouter image
    </a>
</div>

{{ form_row(form.excursionimages) }}
    <button class="btn btn-primary me-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
