<form method="post" action="{{ path('excursion_delete', {'id': excursion.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ excursion.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
