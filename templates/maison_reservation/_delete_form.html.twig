<form method="post" action="{{ path('maison_reservation_delete', {'id': maison_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maison_reservation.id) }}">
    <button class="btn">Delete</button>
</form>
