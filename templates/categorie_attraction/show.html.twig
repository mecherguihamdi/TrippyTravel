{% extends 'baseBO.html.twig' %}

{% block title %}CategorieAttraction{% endblock %}

{% block body %}
    <h1>CategorieAttraction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_attraction.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_attraction.libelle }}</td>
            </tr>
            <tr>
                <th>ContrainteAge</th>
                <td>{{ categorie_attraction.contrainteAge ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin-dashboard/categorie_attraction') }}">back to list</a>

    <a href="{{ path('categorie_attraction_edit', {'id': categorie_attraction.id}) }}">edit</a>

    {{ include('categorie_attraction/_delete_form.html.twig') }}
{% endblock %}
