{% extends 'baseBO.html.twig' %}

{% block title %}CategorieAttraction{% endblock %}

{% block body %}
      <div class="main-panel">
        <div class="content-wrapper">
          <div class="row">
            <div class="grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title"></h4>
                  <div class="table-responsive">
    <table class="table">
    <div style="float:right">
        {{ include('categorie_attraction/_delete_form.html.twig') }}
    <div/>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_attraction.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_attraction.libelle }}</td>
            </tr>
            <tr>
                <th>ContrainteAge</th>
                <td>{{ categorie_attraction.contrainteAge ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
<div/>
<div/>
<div/>
<div/>
<div/>
<div/>
<div/>
<div>
        <a href="{{ path('admin-dashboard/categorie_attraction') }}" class="btn btn-warning" style="float:right">back to list</a>
        <a href="{{ path('categorie_attraction_edit', {'id': categorie_attraction.id}) }}" class="btn btn-warning">edit</a>
<div/>

{% endblock %}
